# Bills

Elite Notes — premium bills storefront built with Next.js, MongoDB, and Tailwind CSS.

## Setup

1. Copy `.env.example` to `.env` and configure MongoDB.
2. `npm install`
3. `npm run dev`

## Admin

- Seed admin: `GET /api/admin-seed`
- Dashboard: `/admin`
